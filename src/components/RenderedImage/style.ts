import { styled } from "@mui/material/styles";

const CustomImage = styled("img")({
  width: "400px",
  height: "400px",
  justifyContent: "center",
  display: "flex",
  margin: "auto",
  marginTop: "50px",
  cursor: "pointer",
});

export { CustomImage };
